# MLPO v1.0 — Dependencies
# Target: Python 3.11+ (tested on 3.11 and 3.14), NVIDIA RTX 3050 (6GB VRAM)

# Core ML
torch>=2.1.0
numpy>=1.24.0
pandas>=2.0.0

# Data
yfinance>=0.2.28

# Convex Optimization (pure Python — no C++ build required)
cvxpy>=1.4.0
# NOTE: cvxpylayers removed — replaced with pure-PyTorch unrolled solver
#       to avoid diffcp C++ compilation (requires MSVC nmake on Windows)

# Hyperparameter Optimization
optuna>=3.4.0

# Interpretability
# shap>=0.43.0  # Optional — install separately if needed

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# Testing
pytest>=7.4.0

# Utilities
scikit-learn>=1.3.0
scipy>=1.11.0
